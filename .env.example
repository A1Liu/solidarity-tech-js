# Solidarity Tech API key for the live-capture scripts (scripts/*.ts).
# Copy this file to `.env` at the repo root and fill in your key.
# `.env` is gitignored; Bun auto-loads it, so `bun run scripts/list-events.ts`
# picks up API_KEY with no extra flags. Never commit the real key.
API_KEY=
